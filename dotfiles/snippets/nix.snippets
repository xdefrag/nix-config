snippet pkg "Packages as variable or import"
{ pkgs ? import <nixpkgs> {} }:

${0}
endsnipet

snippet sh "Nix's shell template"
{ pkgs ? with import <nixpkgs> {} }:

pkgs.mkShell {
	buildInputs = with pkgs; [
		${1:go}
	];

	shellHook = ''
		export PATH=$PATH:~/go/bin
	'';
}
endsnippet
